"use strict";

// /*
// TODO: Create a function below, concatFirstAndLast, that
// takes in an array and returns the first and last inputs
// concatentated together if both elements are strings.
// If either the first or last element does is not a string
// or if the array contains one or fewer elements,
// return false.

function concatFirstAndLast(input) {
    var input = ["square", "rectangle", "circle", "triangle"];
    return input = (array.join(["0" + "3"]));

    console.log(input);
}


//  */
//     Example input/output...
//
//     concatFirstAndLast(['a', 'b', 'c']) // 'ac'
//     concatFirstAndLast([]) // false
//     concatFirstAndLast(['23', '12', 'hello']) // '23hello'
//     concatFirstAndLast([true]) // false
//     concatFirstAndLast([true, 'bob']) // false
//  */
//